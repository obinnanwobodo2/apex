// DRAFT — Portfolio section hidden pending real client project screenshots, descriptions, and results data.
// Do not remove this component. Re-enable by removing the early return below once assets are supplied.
// Heading when re-enabled: "Work We've Done"
// Each card requires: project screenshot, client name/project type, 1–2 line description,
// one measurable outcome, and category tags (E-Commerce, Branding, Web Design, etc.)

export default function LandingPortfolio() {
  return null;
}
